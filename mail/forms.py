from django import forms


#class Email_details(forms.ModelForm):
 #   comment = forms.CharField(label='Comment', widget=forms.Textarea(attrs={'placeholder': 'comment'}))
   # class Meta:
        #model = Email_details

        #fields = ['name','email']